using UnityEngine;

public class WaterScript : MonoBehaviour
{

[Range(2,500)]
    public int NumOfVertices = 70;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
